module.exports = {
  purge: ["./src/**/*.{js,jsx,ts,tsx}"],
  // Enable class-based dark mode so `dark:` utilities work
  darkMode: 'class',
  theme: {
    extend: {},
  },
  variants: {
    extend: {},
  },
  plugins: [],
}
